/api/accounts:
  get:
    summary: ??계좌 목록 조회

    tags:
      - Accounts

    security:
      - cookieAuth: []

    responses:

      '200':
        description: 계좌 목록 조회 ?�공

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: true

                message:
                  type: string
                  example: 계좌 목록 조회 ?�공

                data:
                  type: array

                  items:
                    type: object

                    properties:

                      id:
                        type: integer
                        example: 1

                      user_id:
                        type: integer
                        example: 1

                      account_number:
                        type: string
                        example: 100-1234-567890

                      account_type:
                        type: string
                        example: demand

                      balance:
                        type: integer
                        example: 3000000

                      transfer_limit:
                        type: integer
                        example: 5000000

                      is_active:
                        type: boolean
                        example: true

                      created_at:
                        type: string
                        format: date-time
                        example: 2026-05-14T11:20:30.000Z

      '401':
        description: ?�증 ?�패

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: false

                errorCode:
                  type: string
                  example: INVALID_TOKEN

                message:
                  type: string
                  example: ?�큰 ?�증 ?�패

      '404':
        description: 계좌 ?�보 ?�음

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: false

                errorCode:
                  type: string
                  example: ACCOUNT_NOT_FOUND

                message:
                  type: string
                  example: 계좌 ?�보 ?�음

      '500':
        description: ?�버 ?�류

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: false

                errorCode:
                  type: string
                  example: ACCOUNT_SERVER_ERROR

                message:
                  type: string
                  example: 계좌 조회 ?�버 ?�러


  post:
    summary: ?�출�?계좌 ?�성

    tags:
      - Accounts

    security:
      - cookieAuth: []

    responses:

      '201':
        description: ?�출�?계좌 ?�성 ?�공

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: true

                data:
                  type: object

                  properties:

                    account_id:
                      type: integer
                      example: 1

                    account_number:
                      type: string
                      example: 100-1234-567890

      '400':
        description: 계좌 ?�성 ?�패

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: false

                message:
                  type: string
                  example: 계좌 ?�성 ?�패

      '401':
        description: ?�증 ?�패

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: false

                errorCode:
                  type: string
                  example: INVALID_TOKEN

                message:
                  type: string
                  example: ?�큰 ?�증 ?�패

      '500':
        description: ?�버 ?�류

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: false

                errorCode:
                  type: string
                  example: ACCOUNT_SERVER_ERROR

                message:
                  type: string
                  example: 계좌 ?�성 ?�버 ?�러

/api/accounts/{account_id}:
  get:
    summary: ??계좌 ?�세 조회

    tags:
      - Accounts

    security:
      - cookieAuth: []

    parameters:
      - in: path
        name: account_id
        required: true

        schema:
          type: integer

        example: 1

    responses:

      '200':
        description: 계좌 ?�세 조회 ?�공

        content:
          application/json:

            schema:
              type: object

              properties:

                success:
                  type: boolean
                  example: true

                message:
                  type: string
                  example: 계좌 조회 ?�공

                data:
                  type: object

                  properties:

                    id:
                      type: integer
                      example: 1

                    user_id:
                      type: integer
                      example: 1

                    account_number:
                      type: string
                      example: 100-1234-567890

                    account_type:
                      type: string
                      example: demand

                    balance:
                      type: integer
                      example: 3000000

                    is_active:
                      type: boolean
                      example: true

                    created_at:
                      type: string
                      format: date-time
                      example: 2026-05-14T11:20:30.000Z

      '401':
        description: ?�증 ?�패

      '404':
        description: 계좌 ?�보 ?�음

      '500':
        description: ?�버 ?�류

